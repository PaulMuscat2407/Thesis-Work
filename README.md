For the code to function, create a python enivronment.
After creating the python environment, run the following command to install all necessary packages/dependencies:

pip install -r requirements.txt
